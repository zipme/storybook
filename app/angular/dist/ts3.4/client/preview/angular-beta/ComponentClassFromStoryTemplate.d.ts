import { Type } from '@angular/core';
/**
 * Wraps the story template into a component
 *
 * @param template {string}
 * @param styles {string[]}
 */
export declare const createComponentClassFromStoryTemplate: (template: string, styles: string[]) => Type<any>;
